# 隐私说明

本说明适用于“课件收纳 · 超星课件下载助手”1.0.0。

## 读取与请求

仅在用户使用扫描、复制、下载等功能时，读取允许域名中的课程目录和课件元数据，向课程平台或其文件域名发起必要的读取/下载请求。

扩展使用浏览器已有登录环境，原生请求可能自动携带发往相同平台的 Cookie。扩展不申请 Cookie API 权限，不读取 `document.cookie`，不复制、打印、导出或上传 Cookie。

下载来源兼容规则使用来源页面的 **origin**（协议和域名），不携带课程 URL 中的账号标识和签名参数。规则匹配当前任务的确切文件 URL，仅限无标签页的 GET 下载/请求，并在任务结束时移除。规则为会话规则，不持久保存。

## 本机存储

- `chrome.storage.local`：保存文件夹、并发、重试次数、文件版本等偏好。
- `chrome.storage.session`：保存资源名称、下载 URL、临时签名、课程/章节、来源标签页编号和队列状态。浏览器会话结束时清空，不进行云端同步。
- Chrome 自身：按浏览器规则保存文件、下载记录、访问历史。扩展不主动删除这些记录。

管理页中的“清空列表”只清空课件库；“清理已结束记录”只清理扩展的已结束任务。两者不删除下载文件，不清理 Chrome 下载历史。

## 导出与剪贴板

导出的 JSON 文件清单仅包含文件名、课程名、章节名、类型、扩展名和大小，不包含 URL、资源对象 ID、账号 ID 或临时签名。名称可能仍有个人信息，公开分享前应自行检查。

“复制链接”只有用户点击时才向本机剪贴板写入相应文件 URL，不读取剪贴板。URL 可能含平台临时签名；用户应避免公开分享或将其提交到仓库。

## 不包含的服务

无后端、无账号注册、无广告、无分析统计、无第三方遥测、无远程可执行脚本、无主动上传文件或网络日志的功能。所有应用脚本随项目一起提供。

## 权限范围

站点范围限定为 `chaoxing.com`、`chaoxing.com.cn`、`cldisk.com` 及各自子域名。不读取其他站点内容。正常的文件请求仍可能被平台和网络服务商按其规则记录。

MIT 许可只覆盖本项目代码，不赋予对任何课程或资料的额外权限。
